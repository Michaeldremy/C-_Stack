@model string[]

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    @foreach (var sentence in Model)
    {
        <h4 class="display-4">@sentence</h4>
    }
</div>
